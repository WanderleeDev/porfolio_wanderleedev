---
const formId = import.meta.env.FORMSPREE_FORM_ID;
const endpoint = `https://formspree.io/f/${formId}`;
---

<div class="max-w-2xl mx-auto my-12 p-8 bg-gradient-to-br from-[#FFF9D7] to-[#FFF5C0] dark:from-gray-800 dark:to-gray-900 rounded-xl shadow-xl border border-gray-200/50 dark:border-gray-700/50 backdrop-blur-sm">
	<h2 class="text-2xl font-semibold mb-2 text-gray-800 dark:text-gray-100">Get in Touch</h2>
	<p class="mb-8 text-gray-600 dark:text-gray-300">Do you have a project or want to say hello? Fill out the form and I'll get back to you shortly.</p>

	<form id="contact-form" class="space-y-6" method="POST" action={endpoint} target="_self">
		<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
			<label class="flex flex-col">
				<span class="text-sm font-medium mb-2 text-gray-700 dark:text-gray-200">Name</span>
				<input 
					name="name" 
					type="text" 
					required 
					class="px-4 py-3 rounded-lg bg-white/50 dark:bg-gray-700/50 border border-gray-300 dark:border-gray-600 
					focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent
					transition-all duration-200 hover:border-indigo-300 dark:hover:border-indigo-400" 
				/>
			</label>

			<label class="flex flex-col">
				<span class="text-sm font-medium mb-2 text-gray-700 dark:text-gray-200">Email</span>
				<input 
					name="email" 
					type="email" 
					required 
					class="px-4 py-3 rounded-lg bg-white/50 dark:bg-gray-700/50 border border-gray-300 dark:border-gray-600 
					focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent
					transition-all duration-200 hover:border-indigo-300 dark:hover:border-indigo-400" 
				/>
			</label>
		</div>

		<label class="flex flex-col">
			<span class="text-sm font-medium mb-2 text-gray-700 dark:text-gray-200">Subject</span>
			<input 
				name="subject" 
				type="text" 
				class="px-4 py-3 rounded-lg bg-white/50 dark:bg-gray-700/50 border border-gray-300 dark:border-gray-600 
				focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent
				transition-all duration-200 hover:border-indigo-300 dark:hover:border-indigo-400" 
			/>
		</label>

		<label class="flex flex-col">
			<span class="text-sm font-medium mb-2 text-gray-700 dark:text-gray-200">Message</span>
			<textarea 
				name="message" 
				rows="6" 
				required 
				class="px-4 py-3 rounded-lg bg-white/50 dark:bg-gray-700/50 border border-gray-300 dark:border-gray-600 
				focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent
				transition-all duration-200 hover:border-indigo-300 dark:hover:border-indigo-400 resize-none" 
			></textarea>
		</label>

		<div class="flex items-center gap-4">
			<button 
				type="submit" 
				class="inline-flex items-center gap-2 px-6 py-3 bg-indigo-600 text-white font-medium rounded-lg
				hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2
				transition-all duration-200 transform hover:scale-[1.02] active:scale-[0.98]"
			>
				Send Message
			</button>
			<p id="form-status" class="text-sm text-gray-600 dark:text-gray-300"></p>
		</div>
	</form>
</div>
