---
interface Props {
  customClass?: string;
}

const { customClass } = Astro.props;
---

<strong
  class={`font-black text-xl xs:text-2xl sm:text-3xl md:text-2xl lg:text-3xl text-gray-800 dark:text-gray-100 ${customClass ? customClass : ""}`}
>
  Wanderlee Max
  <span class="text-transparent font-black gradient bg-clip-text"
    >| Web developer</span
  >
</strong>
