---

---

<div
  class="accent-line w-20 h-1 bg-linear-to-r from-cyan-500 via-blue-500 to-purple-500 rounded-full mb-8"
>
</div>

<style>
  .accent-line {
    animation: expandLine 1s ease-out forwards;
    transform-origin: left;
  }

  @keyframes expandLine {
    from {
      transform: scaleX(0);
    }
    to {
      transform: scaleX(1);
    }
  }
</style>
