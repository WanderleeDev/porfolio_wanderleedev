  <ul id="container" class="flex flex-wrap gap-2 justify-center items-center">
    {skills[0].techs.map(({ icon, color }) => (
      <li class="object">
        <div  class="inline-flex items-center gap-2 rounded-full px-4 py-2 shadow-sm border active:cursor-grabbing cursor-grab active:scale-110 transition-transform"
          style={`background-color: ${color}22; border-color: ${color}77;`}
        >
          <Icon name={icon} class="size-6 sm:size-8" style={`color: ${color}`} />
          <span class="text-xs opacity-80 font-medium hidden md:inline">{icon.replace('devicon:', '')}</span>
        </div>
      </li>
    ))}
  </ul>

      <FolderContainer index={index} title={name} backgroundClass={backgroundColor} customClass="h-full">
      <ul class="grid sm:grid-cols-2 md:grid-cols-3 gap-8">
        {
          projects.map(({ title, image, label, demo_url },i) => (
            <a href={demo_url} target="_blank" title={label} rel="noopener noreferrer" >
              <span class="sr-only">{label}</span>
              <CardProject
                index={(i + 1).toString(10).padStart(2, '0')}
                bgCustomClass={backgroundColor}
                thumbnail={`/${image}`}
                name={title}
              />
            </a>
          ))
        }
      </ul>
    </FolderContainer>

  <SubPageLayout title={name} backgroundColor={backgroundColor} index={index}>
